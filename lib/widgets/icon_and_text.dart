import 'package:flutter/cupertino.dart';

class IconAndText extends StatelessWidget {
  const IconAndText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
